interface:
  display_name: "Humanaios"
  short_description: "Repo-specific patterns and workflows for humanaios"
  default_prompt: "Use the humanaios repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true